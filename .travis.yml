language: go
go:
  - 1.12.x
  - master
matrix:
  allow_failures:
    - go: master
env: GO111MODULE=on
install:
  - echo $TRAVIS_GO_VERSION
  - sudo pip install codecov
script:
  - make ci
after_script:
  - codecov
